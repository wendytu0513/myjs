function checktwid(id){
    let ret = false;

    if (id.length == 10){
        let c1 = id.substr(0,1);
        let letters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
        if (letters.indexOf(c1)>=0){
            let word = id.substr(0,2);
            
        }
    }
return ret;
}